# Session 
from django.contrib.sessions.models import Session
s = Session.objects.get(pk='ye8evivr1r9rbdgj8tkkbtqr33t7fr56')
s.get_decoded()

#Pytest
pytest a_directory                     # directory
pytest test_something.py               # tests file
pytest test_something.py::single_test  # single test function
pytest tests/account/test_account.py::test_function_fixture1  -m "views" -rP
pytest -rP
pytest -m "views" -rP
coverage run -m pytest 

# Celery
function.delay()
function.apply_async()
celery -A circle worker -l info #unix
celery -A circle worker -l info --pool=solo #windows

# Flower
celery -A circle flower --address=127.0.0.1 --port=5566 --basic_auth=lakone:a1b2c3

# Beat
celery -A circle worker -B -l INFO

You can also embed beat inside the worker by enabling the workers -B option


Database scheduler
celery -A circle beat -l INFO --scheduler django_celery_beat.schedulers:DatabaseScheduler